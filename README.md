Attempt to solve https://github.com/rollup/rollup/issues/861.

Build...

```bash
npm i
npm run build
```

...then inspect the contents of the `build` directory.

This is kinda messy but is maybe a useful starting point.
